if BOARD_${id.upper()}_LEFT

config ZMK_KEYBOARD_NAME
    default "${shortname}"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif # BOARD_${id.upper()}_LEFT

if BOARD_${id.upper()}_LEFT || BOARD_${id.upper()}_RIGHT

if USB

config USB_NRFX
    default y

config USB_DEVICE_STACK
    default y

endif # USB

config BT_CTLR
    default BT

endif # BOARD_${id.upper()}_LEFT || BOARD_${id.upper()}_RIGHT
